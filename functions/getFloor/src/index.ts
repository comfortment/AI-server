import { Handler } from "aws-lambda";

import { GetFloorRequest } from "../../../types/GetFloorRequest";

const handler: Handler = async (event: GetFloorRequest, _, __) => {
  return;
};

export default handler;
